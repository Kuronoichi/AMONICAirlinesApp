<Window x:Class="AMONICAirlinesApp.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AMONICAirlinesApp"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800"
        MaxHeight="550" MaxWidth="900"
        MinHeight="400" MinWidth="750">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Viewbox Grid.Row="0"
            Margin="100 20 100 20">
            <Image Source="/Resources/Images/DS2017_TP09_color.png" 
                   Stretch="UniformToFill"/>
        </Viewbox>

        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <TextBlock Text="Username:"
                       Margin="10"
                       FontSize="20"/>
            <TextBox x:Name="TextBoxUsername"
                     MinWidth="400"
                     MaxWidth="600"
                     Margin="10"/>
        </StackPanel>
        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                    HorizontalAlignment="Center">
                <TextBlock Text="Password: "
               Margin="10"
               FontSize="20"
              FontFamily="{StaticResource VerdanaRegular}"/>
                <TextBox x:Name="TextBoxPassword"
             MinWidth="400"
             MaxWidth="600"
             Margin="10"
             FontFamily="{StaticResource VerdanaRegular}"/>
            </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button x:Name="ButtonLogin"
                    Content="Login"
                    Width="70"
                    Margin="10"
                    Click="ButtonLogin_Click"/>
            <Button x:Name="ButtonExit"
                    Content="Exit"
                    Width="70"
                    Margin="10"
                    Click="ButtonExit_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="4"
                    HorizontalAlignment="Center">
            <TextBlock x:Name="TextBlockCooldown"
                       />
        </StackPanel>
    </Grid>
</Window>
